import React from React;
import {text, view, image, Button, FlatList} from 'react-native';

const Dog = props => {
  return (
    <view> 
      <image source =(require)
    </view>
  )

};

const Doglist = () => {
  return {
    <view>
      <flatlist>
        data=({
          (name; "John")
          
        })
      </flatlist>

  }
}

export default Dog;